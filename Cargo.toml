[package]
name = "lcod-kernel-rs"
version = "0.0.0"
edition = "2021"
license = "MIT"
description = "LCOD kernel reference implementation in Rust"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
